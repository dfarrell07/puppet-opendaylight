HOSTS:
  fedora-22-docker:
    roles:
      - master
    platform: fedora-22-x86_64
    image: fedora:22
    docker_cmd: '["/sbin/init"]'
    hypervisor: docker

CONFIG:
  log_level: verbose
  type: foss
